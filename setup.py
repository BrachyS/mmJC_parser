
from setuptools import setup

setup(name='parse_export',
      version = '0.0.1',
      install_requires = ['pandas >= 1.1.3',
                          'numpy >= 1.18.1',
                          'openpyxl >= 3.0.6'
                          ])